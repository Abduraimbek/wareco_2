enum Menus {
  box('BOX'),
  pallet('PALLET'),
  vsr('VSR'),
  location('LOCATION'),
  inventory('INVENTORY'),
  maintenance('MAINTENANCE'),
  information('INFORMATION');

  const Menus(this.text);

  final String text;
}
